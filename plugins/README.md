# plugins/

External ingest plugins live here. The plugin interface, loader, and contract
tests are authored in **Story 8.1** (see `!DOCS/planning-artifacts/epics.md`).

For now this directory is intentionally empty — built-in ingest providers ship
inside `src/latestnews/ingest/`. See `../AGENTS.md` for the plugin walkthrough
once it is authored.
